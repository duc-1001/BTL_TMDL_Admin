'use client'

interface EditHeroPageProps {
    id: string
}

export default function EditHeroPage(props: EditHeroPageProps) {
    
  return (
    <div className="max-w-xl space-y-6">
      <h1 className="text-2xl font-bold">Chỉnh sửa Hero</h1>

      {/* load data từ API */}
    </div>
  )
}
